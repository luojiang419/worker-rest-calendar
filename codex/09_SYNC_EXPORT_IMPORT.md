# Phase 09 · 导入导出与可选同步

## 目标

完成数据可迁移能力；云同步用 feature flag 控制。

## 任务

- 导出版本化 JSON。
- 导入预览新增、覆盖、冲突数量。
- 事务导入与失败回滚。
- 可选接入 Supabase：本地优先、软删除、同步队列、冲突处理。
- 提供同步状态和重试入口。

## 退出条件

- 导出→清空→导入可恢复所有核心数据。
- 离线时应用不受影响。
- 云同步关闭时不要求网络或登录。
